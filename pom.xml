name: Java CI with Maven

on:
    push:
    branches: [ main ]
    pull_request:
    branches: [ main ]

    jobs:
    build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
        uses: actions/checkout@v4

    - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
        distribution: 'temurin'
        java-version: '17'

    # Specify the directory where your pom.xml is located (if not in the root)
    - name: Build with Maven
        run: mvn -f path/to/your/project/pom.xml clean install
